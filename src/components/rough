// let position = idx - carouselIndex;
//               // Handle looping for smooth animation
//               if (position < -1) position += carouselProducts.length;
//               if (position > 1) position -= carouselProducts.length;
//               let style =
//                 position === 0
//                   ? 'z-20 translate-x-0 opacity-100'
//                   : position === -1
//                   ? 'z-10 -translate-x-full opacity-0'
//                   : position === 1
//                   ? 'z-10 translate-x-full opacity-0'
//                   : 'hidden';



// import React, { useState, useEffect } from 'react';
// import { motion } from 'framer-motion';
// import { FaFilter } from "react-icons/fa6";
// import { CiSearch } from "react-icons/ci";
// import Header from "./layout/Header";
// import Loader from "./Loader";
// import Choice from '/images/choiceimg.svg';

// // --- Types ---
// interface Product {
//   id: number; // Added ID for better React keys
//   name: string;
//   price: string;
//   image: string;
//   description: string;
// }

// const CAROUSEL_PRODUCTS: Product[] = [
//   { id: 1, name: "Avocado Chicken Salad", price: "$10.00", image: Choice, description: "Product of the day" },
//   { id: 2, name: "Grilled Salmon Bowl", price: "$12.50", image: Choice, description: "Chef's special" },
//   { id: 3, name: "Vegan Buddha Bowl", price: "$9.80", image: Choice, description: "Healthy pick" },
//   { id: 4, name: "Quinoa Power Salad", price: "$11.20", image: Choice, description: "Energizer" },
// ];

// // --- Sub-Component: 3D Slider ---
// const FocusedSlider: React.FC<{ products: Product[] }> = ({ products }) => {
//   const [index, setIndex] = useState(0);

//   useEffect(() => {
//     const timer = setInterval(() => {
//       setIndex((prev) => (prev + 1) % products.length);
//     }, 4000);
//     return () => clearInterval(timer);
//   }, [products.length]);

//   // Determine the position of a card relative to the active index
//   const getPosition = (itemIndex: number) => {
//     if (itemIndex === index) return "center";
    
//     // Logic for "Left" (Previous) item, handling array wrap-around
//     const prevIndex = index === 0 ? products.length - 1 : index - 1;
//     if (itemIndex === prevIndex) return "left";

//     // Logic for "Right" (Next) item, handling array wrap-around
//     const nextIndex = index === products.length - 1 ? 0 : index + 1;
//     if (itemIndex === nextIndex) return "right";

//     return "hidden";
//   };    

//   const variants = {
//     center: {
//       x: "0%",
//       scale: 1,
//       opacity: 1,
//       transition: { duration: 0.8, ease: "easeInOut" }
//     },
//     left: {
//       x: "-110%",
//       scale: 0.85,
//       opacity: 0.4,
//       transition: { duration: 0.8, ease: "easeInOut" }
//     },
//     right: {
//       x: "110%",
//       scale: 0.85,
//       opacity: 0.4,
//       transition: { duration: 0.8, ease: "easeInOut" }
//     },
//     hidden: {
//       x: "0%",
//       scale: 0.5,
//       opacity: 0,
//       transition: { duration: 0.4, ease: "easeInOut" }
//     }
//   };

//   return (
//     <div className="relative w-full h-[400px] md:h-[500px] flex items-center justify-center overflow-hidden">
//       {products.map((product, i) => {
//         const pos = getPosition(i);
//         let zIndex = 0;
//         if (pos === "center") zIndex = 10;
//         else if (pos === "left" || pos === "right") zIndex = 5;
//         else zIndex = 0;
//         return (
//           <motion.div
//             key={product.id}
//             initial="hidden"
//             animate={pos}
//             variants={variants}
//             className="absolute w-[70%] md:w-[60%] max-w-[800px] h-[300px] md:h-[350px]"
//             style={{ zIndex }}
//           >
//             {/* Card Content */}
//             <div className="w-full h-full rounded-3xl bg-[#4A4A6A] dark:bg-white p-6 md:p-8 flex items-center justify-between shadow-[0_20px_50px_rgba(0,0,0,0.3)] border border-white/10 overflow-hidden relative">
            
//               {/* Text Content */}
//               <div className="flex-1 z-10 flex flex-col justify-center">
//                 <span className="inline-block py-1 px-3 rounded-full bg-black/10 dark:bg-gray-100 text-[10px] md:text-xs font-bold text-gray-300 dark:text-gray-500 uppercase tracking-wider w-fit mb-3">
//                   {product.description}
//                 </span>
//                 <h2 className="text-white dark:text-[#4A4A6A] font-bold text-xl md:text-3xl lg:text-4xl leading-tight mb-2 md:mb-4">
//                   {product.name}
//                 </h2>
//                 <p className="text-yellow-400 font-black text-2xl md:text-3xl">
//                   {product.price}
//                 </p>
//               </div>

//               {/* Image Content */}
//               <div className="w-1/2 h-full flex items-center justify-center relative">
//                 {/* Decorative Circle behind image */}
//                 <div className="absolute w-[120%] h-[120%] bg-white/5 dark:bg-[#4A4A6A]/5 rounded-full blur-2xl" />
              
//                 <img
//                   src={product.image}
//                   alt={product.name}
//                   className="w-full h-full object-contain relative z-10 drop-shadow-[0_15px_15px_rgba(0,0,0,0.4)]"
//                 />
//               </div>
//             </div>
//           </motion.div>
//         );
//       })}

//       {/* Navigation Dots */}
//       {/* <div className="absolute bottom-6 flex gap-2 z-20">
//         {products.map((_, i) => (
//           <button
//             key={i}
//             onClick={() => setIndex(i)}
//             className={`transition-all duration-300 rounded-full ${
//               i === index ? "w-8 h-2 bg-yellow-400" : "w-2 h-2 bg-gray-400/50 hover:bg-white"
//             }`}
//           />
//         ))}
//       </div> */}
//     </div>
//   );
// };

// // --- Main Component ---
// const FullMenu: React.FC = () => {
//   const [showLoader, setShowLoader] = useState(true);

//   useEffect(() => {
//     const t = setTimeout(() => setShowLoader(false), 2000);
//     return () => clearTimeout(t);
//   }, []);

//   if (showLoader) return <Loader />;

//   return (
//     <div className="w-full min-h-screen bg-gray-50 dark:bg-[#2D2D44] transition-colors duration-500 overflow-x-hidden">
//       <Header title="Full Menu" description="See All Our Dishes" navbarTitle="Gram Bistro" />

//       <main className="max-w-[1440px] mx-auto pt-24 px-4 md:px-8">
        
//         {/* Search Bar & Filter */}
//         <div className="flex flex-col md:flex-row gap-4 mb-10 items-center justify-between max-w-5xl mx-auto">
//           <div className="relative w-full md:w-2/3 group">
//             <CiSearch className="absolute left-5 top-1/2 -translate-y-1/2 text-gray-400 group-focus-within:text-yellow-400 transition-colors" size={22} />
//             <input
//               type="text"
//               placeholder="Search dishes..."
//               className="w-full pl-14 pr-4 py-4 rounded-2xl bg-white dark:bg-[#32324D] shadow-sm border border-transparent focus:border-yellow-400 outline-none text-gray-600 dark:text-gray-200 transition-all"
//             />
//           </div>

//           <motion.button
//             whileTap={{ scale: 0.95 }}
//             className="w-full md:w-auto px-8 py-4 bg-[#32324D] dark:bg-yellow-400 text-white dark:text-[#2D2D44] rounded-2xl font-semibold shadow-lg flex items-center justify-center gap-3"
//           >
//             <FaFilter />
//             <span>Filter Menu</span>
//           </motion.button>
//         </div>

//         {/* The New Slider */}
//         <section className="mb-12">
//           <FocusedSlider products={CAROUSEL_PRODUCTS} />
//         </section>

//       </main>
//     </div>
//   );
// };

// export default FullMenu;